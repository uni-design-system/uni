import { Meta, Title } from '@storybook/addon-docs/blocks';
import * as Stories from './button.stories';
import { StoryUsage } from '../../../stories/blocks/StoryUsage';
import { ThemeOptions } from '../../../stories/blocks/ThemeOptionsBlock';

<Meta of={Stories} name="Button" />
<Title />

`import { UniButtonComponent } from '@uni-design-system/uni-angular';`

## Overview

Button is a clickable interactive element that triggers a response.

You can place text and icons inside of a button.

Buttons are often used for form submissions and to toggle elements into view.

## Usage

<StoryUsage of={Stories.Primary} exclude={['spinnerBox']} />

## Theme options

<ThemeOptions componentName="button" />

## Accessibility

- A real `<button>` element — native focus, Enter/Space activation, and `disabled` semantics.
- `aria-busy` is set while `loading`; the spinner and symbols are decorative.
