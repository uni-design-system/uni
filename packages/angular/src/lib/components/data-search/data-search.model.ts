import type { Border, ContainerColorToken, Radius, TextColor } from '@uni-design-system/uni-core';

export interface UniDataSearchOptions {
  border: Border;
  borderRadius: Radius;
  color: ContainerColorToken;
  placeholderColor: TextColor;
}
