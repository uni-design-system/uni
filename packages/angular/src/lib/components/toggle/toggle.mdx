import { Meta, Title } from '@storybook/addon-docs/blocks';

import * as Stories from './toggle.stories';
import { StoryUsage } from '../../../stories/blocks/StoryUsage';
import { StoryExample } from '../../../stories/blocks/StoryExample';
import { ThemeOptions } from '../../../stories/blocks/ThemeOptionsBlock';

<Meta of={Stories} name="Toggle" />
<Title />

`import { UniToggleComponent } from '@uni-design-system/uni-angular';`

## Overview

A switch-style control for turning an option on or off with immediate effect —
prefer it over a checkbox when the change applies instantly (settings, feature
switches) rather than as part of a submitted form. It implements
`FormCheckboxControl`, so it binds with the Signal Forms `[formField]` directive or a
direct `[(checked)]` model.

Track and knob colors resolve from the `toggle` theme options; geometry comes
from its `sizes` block, and the checked state wears `checkedColor` — falling back
to the active `variant`. The label's typeface and the gap beside it are theme
options too (`textRole`, `textColor`, `gap`).

`checked` is a `model`. Bind it two-way, or hold the value in your own signal: if
your app declines a change the user made, a one-way `[checked]` expression has not
changed, so Angular writes nothing and the switch keeps the state the user left it
in.

## Naming without printing the name

A switch whose meaning is already on screen beside it — a settings row, a table
cell — needs a name for assistive tech that must not be drawn twice. `labelHidden`
keeps the string in the DOM and takes it out of flow.

<StoryExample of={Stories.HiddenLabel} />

## The row as the hit target

HTML forbids nesting a `<label>`, so a consumer cannot wrap one around a switch.
Project the row's content into the toggle instead: it lands inside the control's
own `<label>`, so the whole row is clickable and keyboard-operable with no click
handler and one tab stop.

<StoryExample of={Stories.RowAsHitTarget} />

## Usage

<StoryUsage of={Stories.Default} />

## Sizes

The `size` input selects a triple from the theme's `toggle` sizes block —
`width`, `height`, and `padding`, the knob's inset. Everything else derives:

| | derived as | at `lg` |
| --- | --- | --- |
| knob diameter | `height - 2 * padding` | 16 |
| knob travel | `width - height` | 20 |
| track radius | `height / 2` | 10 |

Stating width and height per size rather than as one number plus a ratio is
deliberate: real switch designs do not hold a constant proportion across sizes,
so a single ratio can match one size or another but not both.

<StoryExample of={Stories.Sizes} />

## Variants and the checked color

<StoryExample of={Stories.Variants} />

The on-state color is `checkedColor` — an input, or `toggle.behavior.checkedColor`
for a whole app — and falls back to `variant` when neither is set. Prefer the
theme option to repeating an attribute on every switch in a product.

<StoryExample of={Stories.CheckedColor} />

## Signal Forms

It implements `FormCheckboxControl`, so Angular's `[formField]` directive drives
`checked`, `touched`, `invalid` and `required` from the field and writes
interaction back to the model.

```ts
readonly model = signal({ enabled: false });
readonly settings = form(this.model, (path) => {
  required(path.enabled);
});
```

```html
<uni-toggle label="Enable feature" [formField]="settings.enabled" />
```

<div class="tip">

The selector is `[formField]`, not `[field]` — the latter was the Angular 21.0
preview name and no longer exists.

`form()` calls `inject()`, so build it in a component. A Storybook `render`
function is not an injection context and will fail with NG0203.

</div>

<StoryExample of={Stories.FormSignals} />

## Theme options

<ThemeOptions componentName="toggle" />

## Accessibility

- Semantic `<input type="checkbox">` with `role="switch"`, properly associated with its label.
- `labelHidden` hides the label visually while keeping it as the accessible name.
- Projected content sits inside that same `<label>`, so it names and activates the control.
- Space toggles; focus indicators are provided for keyboard navigation.
- Screen readers announce the label and on/off state.

## Do

- Use for instant on/off effects (settings, feature switches)
- Project the row's content when the whole settings row should be the hit target

## Don't

- Use in submitted forms where nothing happens until save — that is a checkbox
- Bind `[checked]` one way and expect a declined change to snap back
