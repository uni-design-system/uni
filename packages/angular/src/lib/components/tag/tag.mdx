import { Meta, Title, Description } from '@storybook/addon-docs/blocks';
import * as Stories from './tag.stories';
import { StoryUsage } from '../../../stories/blocks/StoryUsage';

<Meta of={Stories} />
<Title />

`import { UniTagComponent } from '@uni-design-system/uni-angular';`

## Overview

Compact label for classifying content — categories, states, filters. Tags are
display-only chips that wear the active variant's colors; for removable,
type-to-add chips (a future input variant), see the roadmap.

## Usage

<StoryUsage of={Stories.Primary} exclude={['handleClose']} />
