import { Meta, Title } from '@storybook/addon-docs/blocks';

import * as Stories from './multi-select-dropdown.stories';
import { StoryUsage } from '../../../stories/blocks/StoryUsage';
import { StoryExample } from '../../../stories/blocks/StoryExample';
import { ThemeOptions } from '../../../stories/blocks/ThemeOptionsBlock';

<Meta of={Stories} />
<Title />

`import { UniMultiSelectDropdownComponent } from '@uni-design-system/uni-angular';`

## Overview

Multiple choice behind a compact trigger: a disclosure popover containing a search
field, checkbox options, and a Done action. Implements `FormValueControl` with an
array value for Signal Forms, and shows a summarized selection in the trigger.

The popover surface, radius, and elevation resolve from the
`multiSelectDropdown` theme options.

## Usage

<StoryUsage of={Stories.Primary} />

## Error state

<StoryExample of={Stories.ErrorState} />

## Many options

<StoryExample of={Stories.ManyOptions} />

## Theme options

<ThemeOptions componentName="multiSelectDropdown" />

## Accessibility

- The trigger is a disclosure (`aria-haspopup="dialog"`, `aria-expanded`); the popover contains native focusables Tab reaches normally.
- The search input is labelled; options are real checkboxes announced with their state.
- Escape closes the popover and returns focus to the trigger.
