import * as Stories from './center.stories';
import { Meta, Title } from '@storybook/addon-docs/blocks';
import { StoryUsage } from '../../../../stories/blocks/StoryUsage';

<Meta of={Stories} />
<Title />

`import { UniCenterComponent } from '@uni-design-system/uni-angular';`

## Overview

`Center` is a layout component that centers its child within itself.
`Center` is an extension to the `Box` component and has all the `Box` attributes available to it.

## Usage

<StoryUsage of={Stories.Primary} />
