import * as Stories from './stack.stories';
import { Meta, Title } from '@storybook/addon-docs/blocks';
import { StoryUsage } from '../../../../stories/blocks/StoryUsage';

<Meta of={Stories} />
<Title />

`import { UniStackComponent } from '@uni-design-system/uni-angular';`

## Overview

`Stack` is a layout component used to group elements in a vertical arrangement and applies a uniform space between them.
`Stack` is an extension to the `Box` component and has all the `Box` attributes available to it.

## Usage

<StoryUsage of={Stories.StackedCards} />
