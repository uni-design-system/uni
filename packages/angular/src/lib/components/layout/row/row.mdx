import * as Stories from './row.stories';
import { Meta, Title } from '@storybook/addon-docs/blocks';
import { StoryUsage } from '../../../../stories/blocks/StoryUsage';

<Meta of={Stories} />
<Title />

`import { UniRowComponent } from '@uni-design-system/uni-angular';`

## Overview

`Row` is a layout component used to group elements in a horizontal arrangement and applies a uniform space between them.
`Row` is an extension to the `Box` component and has all the `Box` attributes available to it.

## Usage

<StoryUsage of={Stories.ButtonRow} />
