import { Meta, Title } from '@storybook/addon-docs/blocks';
import * as Stories from './wrap.stories';
import { StoryUsage } from '../../../../stories/blocks/StoryUsage';

<Meta of={Stories} />
<Title />

`import { UniWrapComponent } from '@uni-design-system/uni-angular';`

## Overview

Used to add space between elements and wraps automatically if there isn't enough space.

## Usage

<StoryUsage of={Stories.Primary} />
