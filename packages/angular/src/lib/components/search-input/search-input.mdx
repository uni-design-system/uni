import * as Stories from './search-input.stories';
import { Meta, Title } from '@storybook/addon-docs/blocks';
import { StoryUsage } from '../../../stories/blocks/StoryUsage';
import { StoryExample } from '../../../stories/blocks/StoryExample';

<Meta of={Stories} />

<Title />

`import { UniSearchInputComponent } from '@uni-design-system/uni-angular';`

## Overview

Generic search field wearing the shared input chrome: a decorative leading magnifier,
a clear button while there's a query, and Enter to submit (`search`). `change` emits
debounced as the user types — use it to refresh results or the `suggestions` input.

With `suggestions`, the field becomes an ARIA combobox: entries render in a listbox
under the field, ArrowUp/Down move the highlight, Enter selects (emitting both
`suggestionSelected` and `search`), and Escape closes the list first, then clears the
query. The suggestion list's surface, elevation and radius resolve from the theme's
`searchInput` options, alongside the search/clear symbols.

## Usage

<StoryUsage of={Stories.Primary} exclude={['change', 'search', 'suggestionSelected']} />

## Type-ahead

Feed `suggestions` from your data source on every `change` emission:

<StoryExample of={Stories.TypeAhead} />
