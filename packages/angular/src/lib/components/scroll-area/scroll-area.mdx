import { Meta, Title } from '@storybook/addon-docs/blocks';
import { StoryUsage } from '../../../stories/blocks/StoryUsage';
import * as Stories from './scroll-area.stories';

<Meta of={Stories} name="Overview" />
<Title />

`import { UniScrollAreaComponent } from '@uni-design-system/uni-angular';`

## Overview

A scroll container with themed, consistent scrollbars across browsers — thin
Emotion-styled WebKit scrollbars with a `scrollbar-width`/`scrollbar-color`
fallback. It detects scrollable state via ResizeObserver (the `scrollable` output)
and offers `scrollToTop`/`scrollToBottom` for programmatic control.

## Usage

<StoryUsage
  of={Stories.Primary}
  exclude={['scrollbarClassName', 'contentClassName', 'trackClassName']}
/>
