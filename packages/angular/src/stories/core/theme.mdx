import { Meta, Title, Story } from '@storybook/addon-docs/blocks';
import * as Stories from './theme.stories';

<Meta of={Stories} name="Theme" />
<Title />

## Overview

A theme is one plain object — the `UniTheme` contract — and everything a
component renders resolves from it by **token name**. Nine fields:

- `id`, `name` — identity; `name` is what theme switchers display.
- `colors` — the **sole color source of truth**. Every other color-bearing
  token derives from it.
- `typography` — the numeric type scale; CSS-ready typefaces are derived on
  read — see [Typography](?path=/docs/core-typography--typography).
- `borders` — named border shorthands, derived from `colors` (plus any
  overrides).
- `radii` — corner radius scale, `none` → `max`.
- `shadows` — named elevation shadows.
- `spacing` — the spatial scale, `none` → `xxl`, extensible with your own
  named steps.
- `thicknesses` — line weights: `thin`, `standard`, `thick`.
- `motion` — shared overlay timing: `popup` and `panel`.
- `icons` — icon primitives as data-URI masks — see
  [Iconography](?path=/docs/core-iconography--iconography).
- `components` — per-component theming: a fixed base, state-aware `variants`,
  `sizes`, and named `options` that re-point the token scales above.

## Creating a theme

`createTheme({ id, name, colors, … })` is the assembly line, and **colors is
the only required raw material**: borders and the color-bearing parts of
`components` are built from it (`buildBorders`, `buildComponents`), typography,
spacing, and thicknesses come from the shared base scales, radii and shadows
take the base set unless overridden, and `icons` and `spacing` merge over their
base scales.
`createThemeFromPalette` goes one step earlier — a seed + scheme + category
generates the `colors` record itself, which is how the Theme Builder and the
custom brand flow produce complete themes from a single brand color.

Themes register in the `UNI_THEMES` injection token and apply through
`ThemeService.selectTheme()`; every token below re-resolves live when the
theme changes — try the toolbar.

## Validation & runtime registration

Every theme write is validated against the structural contract in uni-core:
`parseTheme(input)` collects **every** issue (`{ path, message }`, e.g.
`colors.primary is required`) instead of letting a malformed theme render as
silent `undefined` CSS; `assertTheme` is the throwing variant. The required
token sets (`REQUIRED_COLOR_TOKENS`, `REQUIRED_TEXT_ROLES`, …) are exported
for tooling. Because a `UniTheme` is plain serializable data, the same
contract validates a theme fetched as JSON — the basis for theme distribution
and generative-UI flows.

The registry is live: `ThemeService.registerTheme(theme, { select? })` adds a
validated theme (it appears in `themeOptions` and `uni-theme-switch`
immediately), `unregisterTheme(id)` removes it, and `setTheme(theme)`
validates and applies without registering. `ThemeService.theme` is a readonly
signal — all writes funnel through these validated paths.

### Applying a theme fetched at runtime

A theme can therefore arrive as data and be applied without a build step —
from the `generate-runtime-theme` / `get-runtime-theme` MCP tools, or the
theme registry the MCP server hosts:

```ts
const { themes } = await fetch('https://uni-mcp.onrender.com/themes/DarkTheme.json').then((r) => r.json());
const result = themeService.registerTheme(themes[0].theme, { select: true });
if (!result.success) console.warn(result.issues);
```

Those payloads omit the built-in icon set — it is roughly 71% of a serialized
theme, and every uni-core consumer already ships it. `registerTheme` and
`setTheme` restore it on the way in, applying the same `{...BaseIcons,
...icons}` rule `createTheme` uses, so a theme's own icons still win. Outside
Angular, call `hydrateTheme()` from uni-core yourself.

## Colors

Container tokens pair with `on-` content tokens (`primary-container` /
`on-primary-container`); swatches showing **Aa** render their paired content
color, so each pair's contrast is visible at a glance.

<Story of={Stories.Colors} />

## Spacing

The named steps are a doubling scale (2, 4, 8, 16, 32, 64, 128px). Real layouts
are rarely built exclusively on one, so the scale is **open**: a theme may add
steps under any name, and they are valid everywhere a spacing token is taken —
`padding`, `gap`, `marginInline`, and component options.

```ts
createTheme({ id, name, colors, spacing: { tight: '6px', snug: '10px' } });
```

```html
<div stack-layout padding="tight" gap="snug">…</div>
```

Because the scale is open, a mistyped token cannot be a compile error. It is
dropped (an `undefined` CSS value simply does not render) and warns once per
token in development.

<Story of={Stories.Spacing} />

## Radii

<Story of={Stories.Radii} />

## Borders

Derived from `colors` — a theme only overrides these to change style, not to
keep them in sync.

<Story of={Stories.Borders} />

## Shadows

<Story of={Stories.Shadows} />

## Thicknesses

<Story of={Stories.Thicknesses} />

## Motion

Motion is a token, not a component detail: every animated component in the
library points at a named primitive, so retiming them is one edit rather than
a hunt through components.

Tokens are named for what moves, not how fast — a small panel attached to a
control snaps, a larger free-floating surface settles, content growing in
place eases at both ends, a transient message arrives under its own steam, and
a control answering a pointer should feel immediate:

| Token          | Default            | Used by                                       |
| -------------- | ------------------ | --------------------------------------------- |
| `popup`        | 100ms linear, ×0.8 | dropdown, menu, multi-select, combobox, search-input, tag-input, time-input |
| `panel`        | 250ms ease         | popover, callout                              |
| `reveal`       | 350ms ease-in-out  | expand, expand-toggle                         |
| `notification` | 350ms ease-in-out  | alert, snackbar                               |
| `control`      | 300ms ease         | radio, menu-item                              |

`reveal` is a *base* speed rather than a final duration: `uni-expand` scales it
by content height (√-of-height, clamped) so a short region stays snappy and a
tall one isn't rushed, and `uni-expand-toggle` reads the same token so the
chevron turns on the region's clock.

A token carries `duration` (ms), `easing` (any CSS easing function) and an
optional `scale` for panels that grow into place. Slow every popup down, and
soften its curve to match, in one place:

```ts
createTheme({
  id: 'Calm',
  name: 'Calm',
  colors,
  motion: { popup: { duration: 240, easing: 'ease-out', scale: 0.95 } },
});
```

Tokens you don't restate keep their base values, and components may point at
a token of their own — `components.dropdown.options.motion` — so one panel can
differ without forking the scale.

Every animated component reads this scale. The per-component duration options
they used to carry — `callout.transitionMs`, `expand.transitionSpeed`,
`alert.transitionSpeed`, `snackbar.transitionDelay`, `radio.transitionSpeed`,
`menuItem.transitionSpeed` — were removed in 9.0.0. Retime the token instead:
one edit covers every component pointing at it, which is the point of a scale.
To retime just one component, give it a token of its own and point that
component's `motion` option at it.

`uni-skeleton` deliberately stays out: its shimmer is a loop rather than a
transition, so it keeps its own `duration`.

A token with `duration: 0` is the way to switch something off — that is how
`radio` and `menu-item` reach the instant behaviour their old `0` gave.

Motion is always behind `prefers-reduced-motion` where a component applies it
through `motionSafe`: a theme sets how things move for people who want
movement, never whether they move at all.

## Component theming

Each entry in `components` layers on the scales above: `fixed` styles always
apply, `variants` restyle by state or emphasis, `sizes` scale dimensions, and
`options` are the named knobs a theme re-points (a typeface token here, a
radius token there) — every component docs page lists its own under **Theme
options**. This is what makes re-theming a token swap instead of a stylesheet
edit.

### Variants are intents

A variant names *what an action means*; the theme decides how that reads. So the
set is not ours to fix — a design system needs its own vocabulary, and
`Variant` is an open registry you extend by declaration merging:

```ts
declare module '@uni-design-system/uni-core' {
  interface UniVariantRegistry {
    destructive: true;
    subtle: true;
  }
}
```

`variant="destructive"` then compiles wherever a variant is accepted, while
`variant="destructve"` still fails — which an open `string` union could not give
you. Style it like any other variant, under `components.<name>.variants`.

<div class="tip">

**The registry extends; it cannot replace.** Declaration merging has no way to
remove a member, so Uni's twelve names stay legal in your app. If you need to
*enforce* a house set, that is a lint rule rather than a type.

Two names are reserved and always present: **`primary`**, which every component
inherits as its default variant, and **`disabled`**, which the disabled state
resolves to.

</div>

A variant your theme never styled cannot be a compile error once the set is
open, and the miss would otherwise be silent — an absent style simply spreads to
nothing. So the theme service says so once per component and variant in dev,
naming what the theme *does* define, exactly as it does for an unknown spacing
token.

### When the accent is not a single style

`variants` is CSS that gets **applied**. That works when a variant paints one
element, as it does for a button. The selection controls are different: a
checkbox's accent lands on the box outline, the checked and indeterminate
fills, the tick and the focus ring at once. Expressing that as CSS would mean
the theme naming `.checkbox-check` and `.radio-inner` — promoting private DOM to
public contract, so renaming an internal element would break every consumer
theme.

Those components use `variantOptions` instead: per-variant data the component
**reads**, naming the colour role and letting the component place it.

```ts
checkbox: {
  variantOptions: {
    primary: { accent: 'primary' },
    warn: { accent: 'warn' },
  },
}
```

## Roadmap

Custom theme representation and validation (surfacing Theme Builder /
`createThemeFromPalette` output in these docs, and validating hand-written
themes against the contract) is planned as a follow-up to this page.
