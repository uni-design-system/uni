import { Meta, Title, Story } from '@storybook/addon-docs/blocks';
import * as Stories from './carbon-menu.stories';
import { DocsTable } from '../blocks/DocsTable';

<Meta of={Stories} name="Carbon Menu" />
<Title />

## Overview

Uni's menu wearing
[IBM Carbon's overflow menu](https://carbondesignsystem.com/components/overflow-menu/usage/),
built **only** from the `menu` and `menuItem` theme options — no menu-specific
styling in these stories.

Menus are where a component library usually gives itself away: panel radius,
row density, hover tint and the divider treatment are the tells. Here the same
component that renders the default theme's rounded, inset menu renders Carbon's
sharp full-bleed one.

<Story of={Stories.OverflowMenu} />

## Gray 90

<Story of={Stories.DarkGray90} />

## What the theme changes

<DocsTable
  head={['Token', 'Default', 'Carbon']}
  codeColumns={[0]}
  rows={[
    ['menu.borderRadius', 'rounded', <><code>none</code> — Carbon has no rounded corners</>],
    ['menu.paddingVertical', <><code>xs</code> inset</>, <><code>none</code> — rows run edge to edge</>],
    ['menu.minWidth', '184', '160'],
    ['menuItem.height', '38', '40'],
    ['menuItem.typeface', <code>label</code>, <><code>body-2-short</code> (14px IBM Plex)</>],
    ['menuItem.hoverColor', <code>primary-container</code>, <><code>secondary-container</code> (<code>$layer-hover</code>)</>],
    ['menuItem.motion', 'control (300ms)', "'productive' — 110ms, Carbon's duration-fast-02"],
    ['menuItem.variants.warn', 'container tint', <>solid <code>$danger-hover</code> fill</>],
  ]}
/>

Every one of those is a theme value, so an app adopting this look writes no CSS.
The dialog counterpart is [Carbon Dialog](?path=/docs/experiments-carbon-dialog--carbon-dialog).
